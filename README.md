# Cache_Controller_Simulation
A simple finite state machine based cache controller simulation.
## Usage

To run the simulation, clone the repo and execute the following commands in the terminal:

```bash
g++ main.cpp -o main
./main
```